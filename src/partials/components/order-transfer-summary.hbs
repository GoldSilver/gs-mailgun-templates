<h2>Transfer Order Summary</h2>
<p>A <a href="\{{transferOrder.invoice.printableUrl}}">printable invoice</a> is also available in your GoldSilver.com account.</p>
<table class="invoice">
    <tr>
        <td>
            <table class="invoice-items" cellpadding="0" cellspacing="0">
                \{{#each transferOrder.cartItems as |item|}}
                    <tr>
                        <td><img src="\{{item.imgUrl}}" alt="\{{item.name}}"></td>
                        <td>\{{item.name}}<br><br>Quantity: \{{item.quantity}}</td>
                        <td class="alignright"></td>
                    </tr>
                \{{/each}}
                <tr class="total">
                    <td class="alignright" colspan="2">Shipping</td>
                    <td class="alignright">\{{transferOrder.invoice.shipping}}</td>
                </tr>
                \{{#each transferOrder.invoice.additionalLineItems as |lineItem|}}
                    <tr class="total">
                        <td class="alignright" colspan="2">\{{lineItem.name}}</td>
                        <td class="alignright">\{{lineItem.total}}</td>
                    </tr>
                \{{/each}}
                <tr class="total">
                    <td class="alignright" colspan="2"><b>Total</b></td>
                    <td class="alignright">\{{transferOrder.invoice.total}}</td>
                </tr>
            </table>
        </td>
    </tr>
    <tr>
        <td class="alignright">
            <p>All prices in US Dollars (USD)</p>
        </td>
    </tr>
</table>
